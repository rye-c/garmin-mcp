# Copy to .env for local/manual runs. For the daily automated coach session,
# set these as environment secrets on the Claude Code Remote *environment*
# instead (never commit real values) — see README.md "Setup" section.

GARMIN_EMAIL=you@example.com
GARMIN_PASSWORD=change-me
